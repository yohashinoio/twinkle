declare func add(a: i32, b: i32) -> i32;
declare func add(a: i32, b: i32) -> i32;
declare func add(a: i32, b: i32) -> i32;
declare func add(a: i32, b: i32) -> i32;
declare func add(a: i32, b: i32) -> i32;

func main() -> i32
{
  return add(48, 10);
}

func add(a: i32, b: i32) -> i32
{
  return a + b;
}
