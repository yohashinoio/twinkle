sizeof
配列型の添字を省略
関数を自動的に前方宣言（Rustみたいな）
コンストラクタの引数の名前をメンバ変数を同じにしているが、そこをxxx_argみたいに変更する（xxx_でもいいが）
論理演算子
文字列をunicode対応に（文字列リテラルを*i8にするか*charにするか悩みどころ）
Unicodeリテラル
ドキュメントに文字列リテラルの説明
戻り値の型が合わない場合などは、何型が何型に合わないのか出力
コード生成のエラーを分かりやすく（行番号などをエラーに付与）
配列
構造体
クラス
演算子オーバーロード
名前空間
export import
-oオプション
github actions
JITコンパイルのチュートリアルを進める
8、16進数リテラル
optionはdriverでパースするようにして、compile::mainにはvariable_mapを渡す（そうするとドライバからリンカを呼びやすくなる）
入力ファイルのエンコードはUTF8限定
jobオプション
アンインストールする方法を追加 deleteオプション的な
