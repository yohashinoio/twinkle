戻り値の型が配列だった場合エラー（構文解析時にエラーにできるかも）
constで受け取った引数をmutableの引数に渡すことを禁止しよう（関数呼び出し時のmutableチェックをしたい）
配列型の添字を省略
多次元配列
論理演算子（&& ||など）
関数を自動的に前方宣言（Rustみたいな）
コンストラクタの引数の名前をメンバ変数を同じにしているが、そこをxxx_argみたいに変更する（xxx_でもいいが）
文字列をunicode対応に（文字列リテラルを*i8にするか*charにするか悩みどころ）
Unicodeリテラル
戻り値の型が合わない場合などは、何型が何型に合わないのか出力
構造体
クラス
演算子オーバーロード
名前空間
export import
-oオプション
github actions
JITコンパイルのチュートリアルを進める
optionはdriverでパースするようにする（Contextのようなクラスを作って、それをcompilerに渡せば良い）
入力ファイルのエンコードはUTF8限定
jobオプション
アンインストールする方法を追加 deleteオプション的な
