extern printf(fmt: *i8, ...) -> i32;

fn fib(n: i32) -> i32
{
  if (n < 3)
    return 1;
  else
    return fib(n - 1) + fib(n - 2);
}

fn main() -> i32
{
  for (let mut i = 1; i <= 40; ++i)
    printf("%d\n", fib(i));
}
